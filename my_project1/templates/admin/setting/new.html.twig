{% extends 'adminbase.html.twig' %}

{% block title %}New Setting{% endblock %}

{% block body %}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="card">
            <div class="card-body">
                <h1>Create New Setting</h1>

                {{ include('admin/setting/_form.html.twig') }}<br>
                <div class="col-md-2">
                <a class="btn btn-block btn-info" href="{{ path('setting_index') }}">Back To List</a>
                </div>
            </div>
        </div>
    </div>
        {% endblock %}
