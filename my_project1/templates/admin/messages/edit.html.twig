{% extends 'adminbase.html.twig' %}

{% block title %}Contact Form Messages Edit{% endblock %}

{% block body  %}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="card">
            <div class="card-body">
                <h1>Edit Messages</h1>

                {{ include('admin/messages/_form.html.twig', {'button_label': 'Update'}) }}<br>
                <div class="col-md-2">
                    {{ include('admin/messages/_delete_form.html.twig') }}
                </div>
                <a href="{{ path('admin_messages_index') }}">back to list</a>
            </div>
        </div>
    </div>

    {% endblock %}

